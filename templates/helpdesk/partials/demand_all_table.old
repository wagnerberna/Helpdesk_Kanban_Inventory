<div class="container">
    <h2>Lista de chamados:</h2>

    <table class="demand-table">
        <thead>
            <th >ID</th>
            <th>Nome</th>
            <th>Título</th>
            <th>categoria</th>
            <th>Atendente</th>
            <th>Status</th>
            <th>Acessar</th>
        </thead>
        {%for demand in all_demands%}
        <tr>
            <td>{{demand.id}}</td>
            <td>{{demand.user_name}}</td>
            <td>{{demand.title}}</td>
            <td>{{demand.category}}</td>
            <td>{{demand.attendant}}</td>
            <td>{{demand.status}}</td>
            <td><a href="{%url "demand_delete" demand.id%}"><i class="fa-sharp fa-solid fa-trash-can"></i></a></td>          
        </tr>
        {%endfor%}
    </table>

</div>